class Solution:
    def findDisappearedNumbers(self, nums: List[int]) -> List[int]:
        n = len(nums)
        all_nums = set(range(1,n+1))
        present_nums  = set(nums)
        missing_nums = list(all_nums - present_nums)
        return missing_nums
